# CLAUDE.md

Instruções para o Claude Code neste repositório.

## Commits e autoria

Não adicionar atribuição ao Claude nos commits — nada de `Co-Authored-By:
Claude...` nem `Claude-Session:` no corpo da mensagem, e não usar "Claude"
nem `noreply@anthropic.com` como autor/committer. Os commits devem ser
assinados como o autor real do trabalho (o dono do repositório).

## Fluxo de PR

Projeto ainda não está em produção — o dono testa localmente. Pode abrir a
branch, validar (testes/lint/build) e dar merge direto, sem esperar
aprovação manual. Não precisa manter PR como draft esperando review.

## Autonomia

Não pergunte se deve continuar pro próximo item do roadmap — siga direto,
implementando, testando e mergeando, sem pausar pra pedir confirmação.
Só interrompa se travar de verdade (falta uma decisão que só o dono pode
tomar, ou uma ação destrutiva de verdade fora do fluxo normal de PR).
